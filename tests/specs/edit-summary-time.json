[
  {
    "name": "Edit morning summary time via button",
    "steps": [
      {
        "send": { "callback": "settings:summary" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Enter your morning summary time (e.g. 09:00):"
            }
          }
        ]
      }
    ]
  }
]
