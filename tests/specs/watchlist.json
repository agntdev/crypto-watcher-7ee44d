[
  {
    "name": "/watchlist shows empty state",
    "steps": [
      {
        "send": { "text": "/watchlist" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Your watchlist is empty — tap ➕ Add coin to get started."
            }
          }
        ]
      }
    ]
  }
]
