[
  {
    "name": "Edit cooldown duration via button",
    "steps": [
      {
        "send": { "callback": "settings:cooldown" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "How many seconds between alerts for the same coin? (e.g. 3600 for 1 hour)"
            }
          }
        ]
      }
    ]
  }
]
