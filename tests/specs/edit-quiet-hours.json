[
  {
    "name": "Edit quiet hours via button",
    "steps": [
      {
        "send": { "callback": "settings:quiet" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Enter quiet hours as start and end times (e.g. 22:00 07:00):"
            }
          }
        ]
      }
    ]
  }
]
